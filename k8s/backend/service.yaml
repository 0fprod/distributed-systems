apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: distributed-systems
spec:
  selector:
    app: backend
  type: NodePort
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30000
