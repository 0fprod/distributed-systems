# Redundant with packages/database/.env — Prisma CLI reads from there.
# Keep in sync if you use workspace-level tooling that picks up the root .env.
DATABASE_URL=mysql://root:root@localhost:3306/invoices
