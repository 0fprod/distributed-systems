# Terraform state — contains sensitive values, never commit
.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.backup
*.tfstate
*.tfstate.*

# Variables with real credentials — never commit
terraform.tfvars
*.auto.tfvars

# Crash logs
crash.log
crash.*.log
